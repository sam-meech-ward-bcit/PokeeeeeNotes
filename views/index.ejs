<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="/styles.css">

  <title>Pokeee Notes</title>
</head>
<body>

  <h1>Pokeee Notes</h1>

  <form action="createPokeNote" method="post">
    <input type="text" name="title" placeholder="Title">
    <textarea name="content" placeholder="Content"></textarea>
    <button type="submit">Create</button>
  </form>

  <ul>
    <% for (let i = 0; i < pokeNotes.length; i++) { %>
      <li>
        <h2><%= pokeNotes[i].title %></h2>
        <p><%= pokeNotes[i].contents %></p>
        <!-- use a delete http request instead? -->
        <form action="/deletePokeNote" method="post">
          <input type="hidden" name="id" value="<%= pokeNotes[i].id %>">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% } %>
  </ul>
  
</body>
</html>